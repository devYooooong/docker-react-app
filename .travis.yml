sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating ans image with dockerfile"
  - docker build -t csy.0414/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true csy.0414/docker-react-app npm run test -- --converage

after_success:
  - echo "Test Success"